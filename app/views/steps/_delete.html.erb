<%= form_with model: [habit, step], method: "delete", local: true do |f| %>
    <%= hidden_field_tag 'habit_id', habit.id %>
    <%= hidden_field_tag 'step_id', step.id %>
    <%= f.submit "Delete Action" %></div>
<% end %>