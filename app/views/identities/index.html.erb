<h3>Identities</h3>
<%= link_to "Add Identity", new_identity_path %>
<div>
    <% @identities.each do |identity| %>
       <p> <%= render 'identities', identity: identity %> </p>
    <% end %>
</div>

<%= link_to "Return to Profile", user_path(current_user)%>
<%= link_to "Return to Dashboard", dashboard_path(current_user)%>