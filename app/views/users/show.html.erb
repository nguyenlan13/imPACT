<% @user.identities.each do |identity| %>
    <p>
        My PACTS: 
        <div>
            <%= link_to identity.pact_name, identity_path(identity) %> - Identity: <%= link_to identity.description %>
        </div>
    </p>
<% end %>
  <%= link_to "Add Identity", new_identity_path%>