

   
    <%= form_with model: [commentable, Comment.new], local: true do |f| %>
        <%= f.text_area :content, placeholder: "Cheer eachother on !" %>
        <%= f.hidden_field :commentable_id, :value => commentable.id %>
   <%= f.submit "Cheer!" %>
    <% end %>



